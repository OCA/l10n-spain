<?xml version="1.0" ?>
<odoo>
    <data>
        <template id="channel_template">
            <t t-call="website.layout">
                <div class="oe_structure">
                    <h1>
                        <t t-if="channel.channel_type == 'bonificable'">
                            <span t-esc="channel.fundae_code" />
                        </t>
                        <span t-esc="channel.name" />
                    </h1>
                    <div t-if="channel.channel_type == 'bonificable'">
                        <span t-esc="channel.description" />
                    </div>
                </div>
            </t>
        </template>
    </data>
</odoo>
