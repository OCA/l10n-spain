<?xml version="1.0" encoding="utf-8" ?>
<odoo>
    <template
        id="report_invoice_document_line_canon_manager"
        inherit_id="account.report_invoice_document"
    >
        <xpath expr="//th[@name='th_taxes']" position="after">
            <th
                name="th_canon"
                t-if="any(any(tax.tax_group_id.name == 'Canon digital' for tax in l.tax_ids) for l in o.invoice_line_ids)"
                t-attf-class="text-left {{ 'd-none d-md-table-cell' if report_type == 'html' else '' }}"
            >
                <span>Canon</span>
            </th>
        </xpath>
        <xpath expr="//span[@id='line_tax_ids']/.." position="after">
            <td
                t-if="any(tax.tax_group_id.name == 'Canon digital' for tax in line.tax_ids)"
                t-attf-class="text-left {{ 'd-none d-md-table-cell' if report_type == 'html' else '' }}"
            >
                <span
                    t-esc="sum(tax.amount * line.quantity
                                 for tax in line.tax_ids
                                 if tax.tax_group_id.name == 'Canon digital')"
                    t-options="{'widget': 'monetary', 'display_currency': o.currency_id}"
                />
            </td>
        </xpath>
    </template>
</odoo>
