Install `l10n_es_verifactu_oca` first. Configure the company with the Canary
tax agency, a test AEAT certificate, developer and chaining records, then
install this module.

Use `account_chart_update` if fiscal positions already exist so they pick up
the IGIC VERI*FACTU keys.
